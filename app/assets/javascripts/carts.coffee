# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/


# May be added later on when we allow customers to change amount within the cart
# jQuery ->
# 	$("#quantity_input").change (event) ->
# 		amount = parseInt( $(this).context.value, 10)
# 		priceforone = $('.priceOutput')[0].innerText
# 		pricePerItem = parseFloat(priceforone.replace('руб', ''))
# 		$('.totalPrice')[0].innerText = ((amount*pricePerItem).toString()).concat(' руб')