.row
	.col-md-4.col-xs-12
		=form_for([:admin, @category]) do |f|
			%p
				=f.label 'Наименование категории'
				=f.text_field :title, class: 'form-control'
			%p
				=f.label 'Slug категории (можно не заполнять), это идет в УРЛ (ЧПУ-настройка)'
				=f.text_field :slug
			%p				
				=f.label 'Описание категории'
				%h6 Появляется в каталоге, если клиент нажал на данную категорию. Не рекомендуется оставлять пустым
				=f.text_area :description, class: 'form-control'
			%p
				=f.label 'Сортировка категории (чем выше, тем левее категория появится в верхнем меню'
				=f.number_field :orderid, class: 'form-control'
			%p
				=f.label 'Изображение для категории'
				%h6 Не рекомендуется оставлять пустым, появляется в верхнем меню при нажатии на название категории.
				=f.file_field :avatar
			%p
				=f.submit 'Сохранить', class: 'btn btn-success'
				=link_to 'Отменить', admin_path, class: 'btn btn-info'
	.col-md-8