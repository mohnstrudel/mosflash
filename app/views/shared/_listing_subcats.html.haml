-@subcategories.each do |subcat|
	%p
		=subcat.title
		=link_to 'Редактировать', edit_admin_subcategory_path(subcat)
		=link_to 'Удалить', admin_subcategory_path(subcat), method: :delete, data: {confirm: 'Уверен?'}